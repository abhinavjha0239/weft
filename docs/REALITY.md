# Reality ledger — what's proven vs what's scaffolding

*The anti-self-deception document. Every subsystem is rated honestly; a claim
without a cited test is not "done." Update this in the SAME PR that changes a
subsystem's reality. Ratings:*

- **PROVEN** — behavior demonstrated by a cited test against real Postgres
- **WORKS-THIN** — runs end-to-end, known gaps listed
- **PLACEHOLDER** — deliberately fake stand-in; replacement designed
- **DORMANT** — schema/design exists, zero code paths exercise it

| Subsystem | Status | Evidence / gaps |
|---|---|---|
| Event log write+consume | **PROVEN** | `TestCommitOrderGate` (commit-order race defused), `TestCursorAdvance` (monotone cursors, backdated imports) |
| Gateway resume (F-2) | **PROVEN** | `TestMessageEndToEnd`: disconnect → send-while-away → reconnect(last_id) → gap replays; checkpoint heartbeats |
| Ephemeral signals (typing, read-sync, presence) | **PROVEN** | client→server WS frames + in-memory fan-out, never event-logged (ADR-002 P5): typing scoped to channels OR DM conversations (sender gated by their OWN membership view — a non-participant's frame is dropped; targets by theirs), excluding all the sender's devices; read_marker → durable MarkRead + readstate.synced to the sender's OTHER devices only; **presence derived from the connection registry** — first connection broadcasts active (the value renamed from `online` in P-05), only the LAST disconnect broadcasts offline (multi-device proven), REST snapshot at `GET /presence` then live presence.changed. `TestEphemeralSignals` + `TestDMTypingAndPresence` (race-clean). Gaps: presence is per-gateway-process (single-node slice — shared plane arrives with multi-node cells), typing has no server-side auto-expiry (clients time out stale indicators); idle/away is now derived — see the Idle/away presence row |
| Idle/away presence (P-05) | **PROVEN (core)** | tri-state presence over the same in-Hub, per-process registry (the UNLOGGED table stays unused): a connected user is `active` or `idle`, `offline` once their last connection drops. Idle = no inbound frame for `Hub.IdleAfter` (exported, default 10min); a per-hub sweep ticker (interval = IdleAfter/4 capped at 60s → O(connections)/min in prod) demotes active→idle, copying the transition list UNDER h.mu and fanning `presence.changed {state:"idle"}` AFTER releasing the lock (the sweep never broadcasts while holding h.mu). EVERY inbound frame — typing, read_marker, or the new throttled `{"type":"active"}` keepalive — records activity and promotes idle→active; multi-device is "as active as the MOST active connection"; a reconnect resets the idle timer. Only TRANSITIONS broadcast (never per frame). The wire value `online` became `active` across the gateway, `GET /presence` (now a tri-state map `{user_id: state}`, offline absent), the dogfood client (index.html), and the presence test — one commit. `TestIdlePresence` (connect→active, silent→idle with IdleAfter=50ms POLLED not slept, activity-frame→active, disconnect→offline). Gaps: invisible mode (needs a user setting + read-side masking, recorded for later); presence still per-gateway-process (shared plane awaits multi-node cells); idle reflects socket silence only, not OS-level away |
| User status (N-3) | **PROVEN (core)** | ADR-011 N-3's durable object, SEPARATE from presence: `PUT/DELETE /api/v1/status` upserts/clears the caller's OWN emoji + text + optional "clear after" expiry (emoji whitespace/control-free ≤32 chars, text control-free ≤120 chars, at least one of the two required, future-only expiry — every rule 400-tested), the identity module owning the write (profile-adjacent state). The directory (`GET /users`) and batch profiles (`GET /users?ids=`) surface the current status through a LEFT JOIN carrying the expiry clause (`expires_at IS NULL OR expires_at > now()`), so a lapsed status reads as absent with no sweep — `TestUserStatus` (set → visible to another user in BOTH surfaces, update overwrites, backdated expiry filtered from reads, clear deletes the row, validation 400s). NOT event-logged (per-user ephemera, read-state precedent). Gaps: auto kinds 2-5 (in-call/focus/ooo/after-hours) reserved — no lane sets them yet; no live status-change events (readers refresh on next fetch) |
| Outbox rule (write+event, one tx) | **PROVEN** | send path in `server.sendMessage`, asserted via live delivery in e2e |
| Schema (all 10 migrations) | **PROVEN to apply** / **DORMANT in bulk** | applies clean on PG17 + constraint smoke tests; **migrations are embedded in the binary** (`migrations.FS`) so `weftd migrate`/`serve`/`import-zulip` are working-directory-independent, an empty set is a build/runtime error (never a silent no-op), and every DB test package runs the production runner. Automations, files, notifications, compliance tables still have ZERO code exercising them — shape bugs there are undiscovered by definition |
| Auth | WORKS-THIN | bcrypt + hashed bearer sessions, 401 negative tested; **login brute-force rate-limited (PROVEN: `TestAuthRateLimit`)**; profile lookups: `GET /me` (boot identity) + batch `GET /users?ids=` (org-pinned, foreign ids silently absent, deactivated included+flagged, 100-id cap) — `TestUserProfiles`; bootstrap 409s only on a REAL slug collision — infra failures are 500 (`TestBootstrapErrorMapping`, red/green). Invites live: capability tokens (hashed at rest, shown once, never listed), invite_members-gated create/list/revoke; accept is pre-auth (IP-limited) and provisions account + credential + role group + PRE-JOINED channels (private included — the invite IS the authorization) + session in one tx; email pins (case-insensitive), max_uses, expiry, revocation all enforced; ROLE CEILING structural (member/guest only, DB CHECK + validation) — `TestInvites`. **Account self-management (P-29, PROVEN):** every session-minting path (bootstrap, login, invite accept) records the client ip + a single-line, valid-UTF-8, 256-byte-capped user agent as display-only metadata (`TestSessionMetadata`, `TestRequestUserAgent`); `GET /me/sessions` lists the caller's live sessions newest-first with the presenting one flagged `current` (token-hash match via the exported `auth.TokenHash` — raw tokens never stored/echoed; pre-metadata rows read back ""); `DELETE /me/sessions/{id}` is strictly self-scoped and oracle-free (foreign/absent/already-revoked/expired are one indistinguishable 404; the foreign victim's token survives — cross-user revocation pinned red/green) with revoke-current = logout, and `DELETE /me/sessions` signs out everywhere else (`TestSessions`); `PATCH /me {full_name}` renames the caller's own account (trimmed, non-empty, ≤100 runes, control-char free; NOT event-logged — avatar/status precedent) reflected in Me + Profiles (`TestProfileEdit`); `POST /me/password` verifies the current password under a row lock (wrong or missing credential → 403, the token IS valid; concurrent changes serialize so the loser fails), enforces min 8 + the 72-byte bcrypt bound Bootstrap lacks (enforced here only), and swaps the hash + revokes every OTHER live session in ONE tx — old password dead at login, other tokens 401, the presenting one survives (`TestChangePassword`). **Password reset via email (P-35, PROVEN):** `POST /api/v1/password-reset/request {org_slug,email}` is enumeration-safe — ALWAYS 200 `{"ok":true}` (an internal failure is logged, never surfaced, since it is account-independent) — minting+emailing a single-use token (32 random bytes, only its sha256 hash stored in the new `password_reset` table, 1h TTL) ONLY for a live credentialed human, and silently sending nothing for unknown/placeholder/credential-less/deactivated targets or once the user has 3 unused unexpired tokens (throttle). Both routes ride the SAME pre-auth per-IP limiter as login; the mail carries the raw TOKEN (no reset web page yet), who/where only, never message content. `POST /api/v1/password-reset/confirm {token,new_password}` claims the row in ONE tx — the `used_at IS NULL` clause is the single-use race guard (red/green proven: dropping it lets a replayed token succeed) — then upserts the credential, revokes ALL live sessions (mailbox control resets everything, no presenting-session exception), and deletes the user's other outstanding rows; every failure (unknown/expired/used token, deactivated user) is one indistinguishable 401, new-password rules min-8/max-72-bytes checked first. `auth.ChangePassword` additionally deletes the user's `password_reset` rows, so a chosen new password voids any in-flight reset mail. `TestPasswordReset` (full loop incl. every prior session 401 + replayed-token 401, oracle-free unknown email/org + validation sending nothing, throttle=3, expiry, deactivated/credential-less silence, change-voids-token). KNOWN GAP: a websocket authed by a now-revoked session lives until reconnect (the gateway auths at connect; REST dies immediately) — live kick is a queued slice. Gaps: password-reset web page + client link format, placeholder (kind-3) claiming, and org-configurable reset TTL (P-35 client-era/future), session labels/geo, 2FA (M4+), no invite emails (links are copy-paste) |
| Transport quality (errors, limits, middleware) | **PROVEN** | error taxonomy via one mapper (`TestErrorTaxonomy`), per-IP pre-auth + per-user API limiters with bounded memory (janitor), request-id/logging/panic-recovery middleware, 1 MiB body caps |
| Guests (P-5) | **PROVEN (core)** | role rides auth.Identity (one join in FromToken; synthetic runner identities are never guests); guests land in role:everyone only, so members verbs stay shut structurally; send_message/create_thread reseeded to EVERYONE (membership still gates WHERE — the only new principals are guests speaking in their own channels; admins retarget via /admin/verbs). Visibility shrinks to their channels: ListChannels hides even PUBLIC channels, directory + batch profile resolution show only channel-mates (self always), guests can only OPEN DMs toward channel-mates (members may still DM them). `TestGuestVisibility`. Gaps: space/work-item visibility for guests rides the general VisibilityScope work; mention-gating verbs (mention_channel/many) unregistered |
| Permission model | **PROVEN (core)** | (verb,scope)→group resolver with closure lookup, live in the send path. `TestResolverCore` (nested-group transitivity, deny-by-default, allow-after-join), `TestMostSpecificWins` (channel override beats org default). Admin reassignment live: `PUT /admin/verbs` retargets any registry verb to any group at org scope (manage_org-gated, event-logged, upsert) — `TestVerbAssignment` proves a member's access flips off and back as create_channel is retargeted. Gaps: ~7 of ~40 verbs registered; profiles/custom groups have no API; grant∩permission for agents (CC-6) is a hook; read path (Get) still membership-join; item-scope VisibilityScope unenforced |
| Message rendering | **PROVEN (core)** | Portable AST engine live in the send path: CommonMark+GFM (tables/strike/tasklists/autolinks), chat hard-breaks, typed @**name** mentions resolved in-tx (ids in render + event payload), :emoji:, XSS-safe by construction (no raw HTML path; scheme allowlist; image alt text read from child segments — goldmark's deprecated helper replaced) — `TestCommonMarkAndGFM`, `TestXSSNeutralized`, `TestMentions`, `TestRichContentEndToEnd`. Gaps: full emoji data table (20 starter codes), spoiler/math/quote-reply syntax, ADF/mrkdwn import converters (importer milestone), re-render-on-version-bump job |
| Reactions | **PROVEN (core)** | idempotent ensure-present/absent toggle (`PUT`/`DELETE /messages/{id}/reactions/{emoji}`): react-needs-read (the same three-way container ACL as message fetch, oracle-free 404), re-add/re-remove emit nothing, events carry the container so the gateway routes them like messages; viewer-aware aggregates (emoji, count, me, user_ids, first-reacted chip order) attached to `GET /messages/{id}` and the thread page in one grouped query. Zulip importer has carried reactions in since the M1 slice. `TestReactions`. Gaps: custom-emoji registry unwired (names accepted as tokens), no reaction notifications (N-1 keyword/reaction kinds), dogfood chips minimal |
| Saved items (M-6) | **PROVEN (core)** | ADR-007 M-6 personal "save for later" (`saved_item` kind 1): `PUT/DELETE /api/v1/messages/{id}/save` toggle the caller's OWN entry, `GET /api/v1/saved` lists them newest-SAVE first (`created_at DESC`, cap 200) with container ids + a ≤160-rune excerpt. Saving needs read visibility — the same three-way container ACL as a message fetch, oracle-free 404 (private-channel content and nonexistent ids both 404); removal is UNGATED and idempotent (a user can always clear their own row, including a tombstone). A saved message later deleted STAYS as a tombstone (empty excerpt, `deleted:true`) rather than vanishing. Never event-logged, never cross-user visible (read-state precedent — proven: alice's and bob's lists are isolated). `TestSavedItems` (newest-save ordering distinguished from id order via a backdated save, idempotent re-save, DM container ids, tombstone, ungated/idempotent unsave, ACL 404s). List-time ACL re-check (reviewer hardening): losing container access keeps the bookmark but MASKS the excerpt (`accessible:false`) — post-departure content never leaks through a saved list, proven by the leave-then-list case. Gaps: the kind-2 "star" is a later slice; **pins (the other half of the P-02 spec) are NOT shipped — the `pin` table is channel-only (`channel_id NOT NULL`) yet the spec requires DM and space-thread pins, which the schema cannot store without a migration; flagged for review rather than invented** |
| Channel pins (P-02b) | **PROVEN (core)** | shared per-container curation on CHANNEL messages (the 0004 `pin` table is channel-scoped, `channel_id NOT NULL`): `PUT/DELETE /api/v1/messages/{id}/pin` toggle a pin gated by the three-way read ACL (oracle-free 404) AND `administer_channel` on the message's channel — a plain member 403s, a non-member 404s indistinguishably from a missing message, an admin 200s; DM/space messages 400 ("pins are for channel messages"). Idempotent (RowsAffected gate — a re-pin/re-unpin emits nothing); `message.pinned`/`message.unpinned` events carry `channel_id` so the gateway routes them to members like any channel event (pins are SHARED state, unlike the personal saved list). Cap 50 per channel, made EXACT under concurrency by locking the channel row before insert+recount (409 beyond — the 51st never lands). `GET /api/v1/channels/{id}/pins` is member-gated (requireMember — a guest sees pins only in their own channels via the same membership gate, no role branch), newest-pinned first, previews `message_id/author_id/≤160-rune excerpt (the saved-items helper)/pinned_by/pinned_at`; deleted messages drop from the list (the JOIN filters `deleted_at`) AND `DeleteMessage` clears the message's pin rows in the SAME tx so the cap stays honest. `TestChannelPins` (permission matrix, single-event idempotence + payload channel, DM 400, cap-409-that-does-not-land, delete-clears-pin, list order/shape, guest-in-own-channel vs foreign-403). Gaps: DM/space-thread pins need a schema change (the channel-only FK); pin ordering is insertion-order (no reorder), no pin "system message"; no `(channel_id, pinned_at)` index — the 50-cap bounds the sort, add it if the cap rises |
| Drafts | **PROVEN (core)** | private per-user compose state: CRUD strictly owner-scoped (cross-user access 404s, no oracle), optional container hints pinned org-local, never event-logged or visible to others (read-state precedent). `TestDrafts`. Gaps: live cross-device sync (needs user-targeted gateway lane), no autosave semantics client-side |
| Forwarding & quoting (P-03) | **PROVEN (core)** | `POST /api/v1/messages/{id}/forward {thread_id, comment?}` copies one message into another thread as a new message: optional comment + a `> `-quoted copy of the original (capped 1000 runes + …) + a plain `— forwarded from @**Author**` line; `forwarded_from_message_id` records the immediate parent (forwarding a forward chains). BOTH gates run in one tx — the three-way READ ACL on the source (oracle-free 404, deleted → 404) and the FULL send gate on the target (`deliverToThreadOpts`, the normal gated send path with attachment turned off). **Mentions in the quote AND the attribution are neutralized** (a zero-width space breaks the `@**` token) so a forward never notifies the people named in the original nor pings the original author, while the forwarder's COMMENT is left untouched so ITS mentions DO notify. Attachments do NOT re-attach: the quoted `/api/v1/files/{id}` links stay inert text, no new `file_reference` row is created (`attachFiles=false`), and `has_attachment` stays false — a reader downloads only via the F-12 union-ACL. `GET /messages/{id}` gains `forwarded_from` (id or null). `TestForwarding` (cross-channel happy path, quoted-mention silent vs comment-mention notified via ProcessOrg + kindsFor, attribution not pinging the author, 1000-rune truncation, no new file_reference + has_attachment false, forwarded_from surfaced, private-source 404, target send-gate 403). Gaps: `forwarded_from` rides only the single-message Get (list endpoints leave it null); no dogfood-UI forward affordance; neutralizing the attribution rather than pinging the original author is a resolved spec ambiguity flagged for the PR |
| Scheduled sends | **PROVEN (core)** | queue-then-deliver with the send gate run TWICE — schedule time (fail fast) and fire time (revoked access fails the delivery with failed_reason recorded on the row, listed to its author — never silent); due-runner claims FOR UPDATE SKIP LOCKED (multi-node safe, injected clock in tests), delivery in a savepoint so failures mark without losing the claim. The ADR-012 not-yet-sent rule is REAL: pending sends pin their attachments via EntityScheduled references (proven: a 40-day-old unclaimed upload survives the GC sweep), released on delivery/cancel as the live message re-claims. Author-only CRUD. `TestScheduledSends`. Gaps: no failure notification to the author (N-1 lane), no scheduled-send events for device sync |
| Message edit/delete | **PROVEN (core)** | ADR-007 M-3 + F-8: author-only edits (moderation deletes, never rewrites) re-parse through the content engine with the prior content captured as a kind-1 revision and the generated search vector re-indexing itself; the event carries NEWLY-added mention ids so the notification pipeline pings only them (tested: edited-in user notified, original mention not re-pinged). Delete = revision-append + live-row scrub (kind-4 capture; emptied source auto-deindexes): author always, `moderate_messages` (default role:moderators) for others' CHANNEL messages, DM/space messages author-only. Frozen-archive rule honored; per-message row lock serializes revisions — `TestMessageEditDelete`. Gaps: no edit window policy knob, revision-history read endpoint, tombstone rendering (deleted messages vanish), reaction cleanup on delete, GDPR scrub job for revision rows (compliance lane) |
| Message move (P-04) | **PROVEN (core)** | Zulip's "move message" as revision kind 3: `POST /api/v1/messages/{id}/move {thread_id}` relocates a CHANNEL message to another thread in the SAME channel (v1 intra-channel only — a cross-channel target 400s "target must be in the same channel"). One tx locks the message (the delete/edit `loadForWrite` shape), authorizes (the author OR `moderate_messages` through the channel chain — a plain member 403s), records a kind-3 `message_revision` carrying `prev_thread_id`, repoints `message.thread_id`, fixes BOTH threads' denormalized counters (source −1, target +1 with `last_activity_at` bumped; kind-2 channel-root threads carry none per F-15 so the `kind=1` filter no-ops them), and appends `message.moved` (payload: message_id, from/to_thread_id, channel_id — the existing gateway filter routes by channel and clients reload both threads). DM/space messages 400 (channel-only), a thread's root message 400 (it anchors the thread), the same-thread target 400. `channel_id` is unchanged, so the content-based search index and the channel read ACL are untouched. `TestMoveMessage` (author + moderator move, member 403 that does NOT relocate, cross-channel/DM/root/same-thread 400s, both counters, the kind-3 + prev_thread_id revision, the event payload, and exactly-two-events proving rejections stay silent). Gaps: cross-channel move (needs a channel_id + reference ACL rewrite); per-thread read watermarks mean a moved message can read as unread in the target (Zulip has the same wrinkle — accepted v1); move events are member-scoped |
| Gateway ACL filter | WORKS-THIN | membership-set filter + refresh on membership events. Missing: history_mode/protected floor, VisibilityScope, the client-side snapshot-refetch rule (no client exists) |
| Channels (CRUD) | **PROVEN (core)** | create (public/private, root thread, creator joins, name-collision 409), discoverability listing (private hidden from non-members), self-join public / 403 private, leave-revokes + re-join (history_from preserved — row reactivation), member.joined/.left events — `TestChannelCRUD`. **Lifecycle** (`PATCH /channels/{id}`, administer_channel-gated): rename with **F-22 alias reservation** — the old name is reserved for its channel (create and rename onto it 409; only the owner takes it back, flipping the reservation); archive freezes writes (send/thread-create 400, join 404) while member history stays readable, and the name frees by design (partial index) so unarchive re-checks and 409s on squatters; description edit; renamed/archived/unarchived/updated events — `TestChannelLifecycle`. Gaps: alias release endpoint (deliberate admin action, F-22), convert public↔private, invitations to private channels, per-channel admin (Channel Manager verb), public/private create-verb split, workspace picker UX, lifecycle events are member-scoped (non-members' discoverable list refreshes on next fetch) |
| Channel folders + default channels (P-09) | **PROVEN (core)** | manage_org-gated admin surfaces, all scoped to the org's bootstrap workspace (resolved server-side — the API is workspace-implicit, the one documented v1 reduction; the `default_channel.bundle` C-3 column stays NULL/DORMANT). Folders: `POST/GET/PATCH/DELETE /api/v1/channel-folders` (name 1–60 trimmed, append-order position, `folder.created/updated/deleted` events on the new `EntityChannelFolder`); hard delete nulls member channels' `folder_id` in the same tx. `PATCH /channels/{id}` gains a tri-state `folder_id` (absent = keep, null = clear, id = assign; a foreign/cross-workspace folder → 400), and `GET /channels` surfaces `folder_id`. Defaults: `PUT/GET /api/v1/default-channels` replace-set (≤20, each a PUBLIC live channel in the workspace — private/archived/foreign → 400, atomic rollback on any failure) — `TestChannelFolders`. **Consumer (honest rungs):** bootstrap seeds #general as a default; on invite accept a new MEMBER auto-joins the workspace's default channels (deduped against the invite's explicit list, each with a `member.joined`), while a GUEST gets ONLY the invite's explicit channels (P-5) — `TestDefaultChannelsOnInvite`. The join is GUARDED at accept time (review hardening): a channel archived after being listed — as a default OR on an invite — silently takes no member and emits no event, while provisioning still succeeds (`TestArchivedChannelsSkippedOnAccept`, red/green-proven). Gaps: no per-workspace API (single implicit workspace), bundle-scoped default groups dormant, no folder reordering endpoint, folder events deliver org-wide (not workspace-filtered), defaults are consumed only at invite-accept (no retroactive join / live default-set events) |
| Sidebar prefs (P-14) | **PROVEN (core)** | wakes the dormant `channel_member.pinned`/`color` columns (the C-4 quartet, dormant since 0003): `PUT /api/v1/channels/{id}/sidebar {pinned,color}` replaces BOTH of the caller's OWN presentation flags in ONE gate-in-the-write UPDATE (PUT-replace, not merge — a missing field takes its zero value; clients send current values). The membership gate lives in the UPDATE's WHERE — an org-pinned channel join plus the caller's own LIVE membership row (`cm.user_id = $ AND unsubscribed_at IS NULL`): 0 rows → an oracle-free 404, so a non-member, an unsubscribed former member, and a foreign-org channel id are one indistinguishable "channel not found" (a sidebar PUT never confirms a channel exists; red/green proven by dropping the liveness predicate, which lets a departed member's row write). color `""` clears to NULL, else must match `^#[0-9a-f]{6}$` case-insensitively and is stored lowercase → else 400 "color must be #rrggbb"; pinned is a plain bool. `GET /channels` surfaces `pinned` + `color` (`""` for NULL) per row; ordering is UNCHANGED (clients sort pinned-first — backend-first honesty). Flags are strictly PERSONAL (a second user's are independent — never shared, never event-logged, the read-state precedent) and ride along through channel rename/archive (archived channels aren't excluded). `TestSidebarPrefs` (set→list reflects both + lowercased, clear-color, bad colors 400, same-org non-member / unsubscribed / foreign-org 404, per-user independence, guest pins own channel). Gaps: named custom sidebar sections need new schema (dormant); server-side pinned-first ordering (clients sort); folder/section client interaction (client era) |
| Direct messages | **PROVEN (core)** | dm_space container (ADR-008): create-or-get by canonical participant key (no locks — the unique index arbitrates), 1:1/group/self kinds, root thread per conversation, `POST /dms` + `GET /dms` (recency-ordered, LATERAL last-message probe), unread counts + mark-read on the same watermark model, live gateway fan-out to participants only (dm.opened decides from its participant list so the invited side hears it before its view refreshes). **Participation IS the permission**, and the inverse is enforced everywhere: a non-participant org member gets no fan-out, 404 on fetch, thread read, send, AND mark-read (P-33 made `requireParticipant` oracle-free — NotFound with body "conversation not found", never echoing the dm_space_id, matching the single-message Get), zero search hits — `TestDirectMessages`. **Group leave (P-08, HARD-LEAVE)**: `DELETE /api/v1/dms/{id}/participants/me` hard-DELETEs the caller's OWN dm_participant row — since every read/fan-out ACL site gates on `EXISTS (dm_participant ...)`, the leaver is excluded everywhere automatically with ZERO predicate edits (no left_at column, no migration): `GET /dms` drops it, thread read/send/mark-read 404, single-message fetch 404, fan-out skips them (proven Bob-leaves: no notification row while Carol's lands). Group (kind 2) ONLY — 1:1/self 400. The dm_key is PRESERVED (still the original sorted-id set), so re-opening the same set via create-or-get resolves THIS conversation and an ensure-actor `INSERT ... ON CONFLICT DO NOTHING` re-adds ONLY the returning caller onto the full history (rejoin, not a new space). `dm.participants_changed` (payload user_ids:[leaver]) rides the SAME gateway user_ids-scan path as dm.opened so the leaver's view refreshes live. Leave is oracle-free 404 (foreign-org/nonexistent/non-participant all indistinguishable). `TestGroupDMLeave`. Zulip DM import lands in the same containers (canonical-key interop); DM typing + presence live on the signal plane. Gaps: no leave for 1:1/self (by design); no remove-OTHER-participant (self-leave only); no "X left" system message; no per-conversation naming for groups (the P-08 DM-thread 403/404 inconsistency is now RESOLVED — see the oracle-free note above, P-33) |
| Threads | **PROVEN (core)** | create (titled/untitled, root message in one tx), retitle, resolve/reopen (idempotent), keyset-paginated list (root excluded), thread messages with before-cursor; F-15 root protections and the create_thread/edit_thread_title/resolve_threads verbs proven end-to-end (`TestThreadLifecycle`, incl. channel-scope verb override). Gaps: move/propagate between channels, DM/Space-governed threads, thread subscriptions (follow/mute), thread events not yet consumed by any UI |
| Read state | **PROVEN (durable core)** | F-7 watermark live: monotone clamped mark-read (`POST /threads/{id}/read`), per-channel unreads excluding own messages (`GET /unreads`) — `TestReadWatermarks`. Deliberately NOT event-logged (highest-volume action; scale contract). Gaps: per-thread unread breakdown, mention badges, F-17 materialized counters at scale |
| Search (messages, v1) | **PROVEN (core)** | `GET /search`: Postgres FTS over a GENERATED search_tsv column (native + imported messages auto-indexed), ADR-010 operator subset (from: by name/email OR bare author id, in:, has:link/has:attachment/has:image, is:resolved/is:unresolved, is:dm, before:/after: + quoted phrases; unknown has:/is: values fall through to literal free text, never a 400), read ACL matching message fetch (channel membership; DM messages participant-only — **is:dm ANDs onto this per-viewer clause, never bypassing it, so a caller sees only DM hits from their own conversations**; **item descriptions/comments org-visible and searchable**, results tagged with item_key/space_id — promoted channel threads carry both scopes), ts_rank ordering + plain-text highlighted snippets; parser unit-tested + `TestSearch` + `TestSearchMoreOperators` (FTS, every operator alone + combined, is:dm caller-scoping, from-by-id, unknown-operator-as-text, ACL/guest isolation, item-hit enrichment, empty-query 400). Client search box browser-verified. Gaps: item TITLES not searched (messages only), is:resolved reflects thread resolution not item status, full JQL-style work-item search, was:/changed: temporal (event-log), pgvector semantic lane, public-non-member visibility, cross-entity (files/people/channels) |
| Link previews (P-15) | **PROVEN (core)** | unfurl: a named txid-gated event-log consumer on message.created extracts the first 2 external http(s) links from the message AST (self-host links skipped) and fetches page metadata EXCLUSIVELY through `platform/egress` — the SSRF-guarded client: resolve-once-dial-pinned (the dialer vets every resolved address and dials only the vetted literal, closing the DNS-rebinding TOCTOU), loopback/RFC1918/ULA/link-local (incl. 169.254.169.254 metadata)/unspecified/multicast/broadcast and their IPv4-mapped-v6 disguises all rejected, mixed public+internal answers rejected whole, scheme http(s)-only, ports 80/443, no userinfo, max 5 redirects with EVERY hop re-vetted, env proxies disabled, fixed UA, no credentials ever. The SSRF matrix is a network-free unit suite (`TestVetAddr` + friends, red/green: neutering vetAddr fails every class) plus an e2e pin (a metadata-IP link lands status 3 disallowed under the loopback test option — `TestLinkPreviews`). Results cache GLOBALLY by url_hash (ok 24h / failed 1h / disallowed 24h — failures and guard verdicts are cached, never refetch-looped); og:*/twitter:*/<title> parsed by x/net/html tokenizer from ≤1 MiB of body, title/description/site_name capped + control-flattened, og:image kept as an absolute-http(s) STRING only (never fetched server-side — client-era camo is the gap; rendering it raw would leak reader IPs). Associations land with a `message.preview_added` event (system actor, message-container routing) in one tx; the read surface (`link_previews` on ListMessages/Get, status-1 rows only, document order) rides the message ACL — previews add NO visibility surface. Org toggle `PUT/GET /api/v1/admin/link-previews` (manage_org, jsonb_set, event-logged; absent = enabled; off = events consumed not deferred, proven zero fetches). Fetches live ONLY in the consumer lane — the send path never waits on a remote page. Gaps: image camo/proxy; un-unfurl on edit; per-message/user opt-out; charset sniffing beyond UTF-8; oEmbed; P-24 shares the egress guard |
| Web-public channels + protected history (P-16) | **PROVEN (core)** | channel.visibility value 3 (web_public) and history_mode value 2 (protected) get their first writers — zero migrations, the columns waited since 0003. Create: `visibility` string param ("public"/"private"/"web_public"; empty falls back to the legacy `private` bool, non-empty wins), `protected` valid ONLY with private (public discovery and world-readability both contradict a per-member boundary — either mismatch 400s); web_public self-joins like public, lists org-wide, event payload carries both spellings. **The read gate diverges from the send path BY DESIGN** (the audit finding: requireThreadSend CALLS requireMember): `requireChannelRead` = live membership OR (visibility=3 AND archived_at IS NULL) on ListThreads/ListMessages/Get ONLY; every write path keeps requireMember — red/green: pointing the send gate at the read gate lets a non-member post and `TestWebPublicChannels` fails at the 403 pin. Guests read web-public (world-readable trumps P-5) but their listing stays memberships-only; archiving closes the non-member branch while members keep history. **Anonymous surface**: `GET /api/v1/public/{channels/{id}, channels/{id}/threads, threads/{id}/messages}` behind a DEDICATED per-IP bucket (5 rps burst 20 — never sharing the login brute-force knob), NO identity resolved; the Public* domain methods pin `visibility=3 AND archived_at IS NULL` in SQL with the paging queries JOINing the same predicate (defense in depth — red/green: widening the pin leaks a private thread and the byte-identical-404 oracle in `TestPublicChannelAnon` fails). Anonymous projection is a separate type: id/thread/author/source/rendered/created_at + link_previews; **reactions EXCLUDED** (ReactionAgg names reactors); authors map bounded to the returned messages' authors, never the directory; deleted messages invisible; POST verbs don't exist under /public and the authed API 401s (asserted). **history_from**: stamped by identity.joinChannelOnAccept via `CASE WHEN c.history_mode = 2 THEN now() END` — the only membership-insert site reaching protected channels (protected⟹private⟹invitation; JoinChannel can never apply); creator NULL = full history; ON CONFLICT DO NOTHING never restamps a preserved row; leave preserves the stamp (C-4). ListMessages resolves the viewer's boundary in-tx (one indexed row) and filters `created_at >= $boundary` (NULL disables); Get grows a NOT EXISTS arm so pre-join messages are oracle-free 404s; thread SUMMARIES stay visible (the boundary hides content, not the conversation's existence). Red/green: neutering the filter shows the newcomer pre-join history and `TestProtectedHistory` fails. Gaps: web-public pins/files/search/read-state stay member-only; public reaction counts; anon avatar bytes; anon live updates (poll only); org policy knob for who may create web-public; robots/SEO/cache headers (client era); protected settable only at creation; private-channel member-add/rejoin endpoint (the rejoin contract is asserted by reactivating the row in the JoinChannel shape) |
| Work items (the fusion) | **PROVEN (core)** | Spaces (seeded To Do/In Progress/Done + Task/Bug/Epic types + rank context), items owning space-governed threads (description = root message, one content system), **thread→item promotion keeping the SAME thread with unchanged channel ACL (D2 + F-5)**, comment-on-item = reply-in-thread via `POST /threads/{id}/messages`, **resolution DERIVED from status category incl. clearing on reopen (W-3)**, cross-space status ids rejected, per-space advisory-locked key_no, keys never reused (409) — `TestWorkItemFusion`. Message fetch-by-id shares the ListMessages visibility rule (space-thread messages org-visible), so live item comments render in the client. Gaps: org-wide space visibility slice (no VisibilityScope/profiles yet), sprints (P-11), board reordering (P-12), and custom fields + links (P-13) now live (see their rows), no epics hierarchy endpoints, promotion consent ceremony for broader-visibility Spaces not yet surfaced (all spaces org-visible so no exposure delta yet) |
| Sprints (P-11) | **PROVEN (core)** | The dormant `sprint` table + `work_item.sprint_id` (both since migration 0005, no writers until now) get their writers — a sprint is a COLUMN on the item (Jira's active-sprint model: one sprint per item), and every mutation gates on org-scoped `edit_items`. `POST /spaces/{id}/sprints` mints a state=1 (future) sprint (name trimmed 1..100 runes, goal ≤2000, and ends_at>starts_at only when both are present — else 400; the space is `loadSpace`-gated so foreign-org/archived/trashed is an oracle-free 404), event `sprint.created` {sprint_id, space_id, name} under the new `EntitySprint`=19. `GET /spaces/{id}/sprints` lists them state ASC / id DESC with item_count from ONE grouped `count(*) FILTER (WHERE trashed_at IS NULL)` LEFT JOIN (no N+1), org-pinned like the sibling ListItems so a foreign/nonexistent space is just the empty set. `POST /sprints/{id}/start` (1→2 only, else 400) stamps starts_at (body value or now) + ends_at-if-given and enforces ONE ACTIVE SPRINT PER SPACE (a second start 409; two different spaces each active is fine), event `sprint.started`. `POST /sprints/{id}/close` (2→3 only, else 400) stamps completed_at and CARRIES OVER unfinished items (`resolved_at IS NULL AND trashed_at IS NULL AND sprint_id = this`) in ONE UPDATE to `move_to_sprint_id` (which must be same-space and not itself closed, else 400; the sprint being closed as its own target 400) or to the backlog (`sprint_id = NULL`) when absent — FINISHED items keep their sprint_id (sprint history is the report), trashed items never carry over, items in OTHER sprints are untouched; the moved count rides `sprint.closed` {sprint_id, moved, moved_to}. `PATCH /items/{id}` gains `sprint_id` (0 clears — the AssigneeID precedent) validated in the SAME loadItem tx (the sprint exists, belongs to the item's space, and is not closed, else 400; the space pin folds cross-space and foreign-org into one 400) and rides the existing `workitem.updated` event. Foreign-org sprint/space is an oracle-free 404 across create/start/close (`loadSprint`/`loadSpace` are org-pinned). `TestSprints` (lifecycle create→start→close with date stamps + events; the one-active 409; carry-over to the named target with finished-stay [RED/GREEN pin: dropping `resolved_at IS NULL` moves finished too and the "finished stay" assert fails] + a second close to the backlog NULL; trashed excluded from item_count; assign/clear incl. closed-sprint 400 and cross-space 400; foreign-org 404s). Gaps: per-space sprint-admin verb (sprint CRUD rides `edit_items` org-wide); sprint reports/velocity/burndown; auto-create-next-sprint on close; `workitem.sprint_changed` as a first-class automation trigger; no sprint rename/edit/delete; sprint_id not yet surfaced on the item read API |
| Board ordering + saved views (P-12) | **PROVEN (core)** | `POST /api/v1/items/{id}/move {after_item_id?,before_item_id?}` mints a rank strictly between the named neighbours (LexoRank over `work_item.rank` TEXT COLLATE "C", F-21) so a reorder rewrites exactly one row; `rankBetween(lo,hi)` takes an a..z midpoint with "" start/end sentinels and preserves any shared prefix, so the digit-bearing ranks CreateItem still mints ride through unchanged — unit-tested incl. the adjacent-with-no-midpoint !ok cases that drive a rebalance. VerbEditItems-gated; ONE tx locks the whole rank_context FOR UPDATE (concurrent moves serialize), backfills any pre-P-12 NULL ranks with evenly spaced ranks in id order first, and on an exhausted gap respaces the whole context to even 3-char ranks and retries once. `ListItems` now orders `rank NULLS LAST, id`, so board order surfaces everywhere items list. Neighbours must be live, org-local, and share the moved item's rank_context (cross-context 400, missing/other-org 404 oracle-free; neither neighbour 400); `workitem.reordered` {item_id,after,before} appended in-tx. `TestBoardOrder` (reorder+live re-query, NULL backfill, forced rebalance+retry, cross-context 400) + `TestRankBetween`. Saved views (the dormant view_def table since 0005) are PERSONAL boards = a saved query + layout: `POST/GET/PATCH/DELETE /api/v1/views`, owner-scoped so a foreign or other-owner view is an oracle-free 404 (never listed for anyone else). A view is {name 1..100, layout 1 list/2 kanban/3 timeline/4 saved-search, query, config}; the query is `{"filters":[{field,op,value}]}` with field in {status_id,type_id,assignee_id,sprint_id,label,flagged} and op in {eq,in}, validated at WRITE (unknown field/op, bad layout/name → 400 on POST and PATCH). space_id must be org-local (404) and is folded into the stored query JSON (view_def has no space_id column). ListItems is NOT auto-filtered by a view. `TestSavedViews` (CRUD, folded space_id round-trip, validation 400s, owner-isolation 404s). Gaps: server-side view execution (filter → ListItems); shared/team views; cross-space/cross-context moves; timeline/kanban server support beyond storage; the dogfood UI has no within-column drag-reorder or saved-view surface yet |
| Custom fields + item links (P-13) | **PROVEN (core)** | ADR-009 W-2's ONE typed field system + ADR-008 C-6 links, ZERO migrations (`field_def`, `work_item.fields` JSONB, `link_type`/`work_item_link` have existed since 0005). **Field defs** (`fields.go`; writes gated `edit_items` org-wide, GET an org-scoped read like ListItems): `POST/GET /api/v1/spaces/{id}/field-defs` + `PATCH/DELETE /api/v1/field-defs/{id}` — a Go registry of the full schema taxonomy enables the v1 subset (text_short/text_long/number/date/checkbox/select/multi_select) and 400s every other type until its validator lands; `key` matches the schema CHECK `^[a-z][a-z0-9_]{0,62}$` and is UNIQUE per space (pre-checked → 409, the CreateChannel precedent); select/multi_select require a non-empty `options.choices` string array (400); `applies_to` must name the space's OWN item types (400); `key` + `field_type` are IMMUTABLE (a type change would strand stored values) while name/required/options/position stay mutable; DELETE removes the def only and leaves stored values as inert orphans (no mass UPDATE → O(1)); `field_def.created/updated/deleted` on the new `EntityFieldDef` (20). **Values** woven into CreateItem/UpdateItem via a new `Fields map[string]any`: the space's defs load ONCE per write and each supplied value is type-validated (`fieldValidate` — unknown key 400, wrong type 400 for string→number / non-choice→select / non-array→multi_select), stored into the GIN-indexed `work_item.fields` and surfaced back on the item read; UpdateItem MERGES (absent key = leave, explicit null = clear) and the `workitem.updated` payload lists the changed field KEYS only, never values (the mention-id precedent). **Item links** (`links.go`; gated `edit_items`): `POST/GET /api/v1/items/{id}/links` + `DELETE .../links/{link_id}` from EITHER endpoint — ONE row per relationship rendered both ways via the link_type inward/outward phrases (never two rows), both items + the type org-local (foreign → oracle-free 404), self-link 400, `UNIQUE(from,to,type)` → 409; keyed by stable internal ids so a link survives a status/rank change on either item (asserted); `workitem.linked/unlinked` events. System link types (blocks / is blocked by; relates to) are seeded per org inside CreateSpace alongside status_set/item_type — ON CONFLICT since they live once per org while CreateSpace runs once per space (the single seeding path, not a second one). `TestCustomFields`, `TestItemLinks`, `TestFieldValidate` (per-type table; red/green: dropping the number type check lets a string into a number field). Gaps: required-field presence enforcement; the remaining taxonomy types (datetime/radio/user/version/cascading/…); a per-space admin verb (the whole module rides `edit_items` org-wide); orphan-value strip-sweep on def delete; applies_to enforcement at value-write (v1 scopes display only); link graph / blocked-by rollups; cross-space consent ceremony (allowed today since edit_items is org-wide); no link-type discovery endpoint yet (link_type ids known to clients out of band) |
| Notification email medium (N-1 step 4) | **PROVEN (core)** | per-(kind) email knobs (`GET/PUT /notification-prefs`; zero rows = Zulip defaults: dm+mention+keyword on — setting an alert word IS the opt-in, and the worker's zero-rows SQL fallback now matches that default (`TestEmailKeywordDefault`, red/green); in-app is STRUCTURAL — the badge accrues even when delivery is suppressed, proven — and push is honestly reserved) + the offline lane: unseen notifications older than the delay (2min default) become ONE digest per user via the `platform/mail` seam. The `mail.Sender` now speaks a `Message{To,Subject,Text,HTML,ListUnsubscribe,ListUnsubscribePost}`: the smtp driver builds multipart/alternative when HTML is set (text/plain FIRST per RFC 2046, crypto-random boundary) and emits the RFC 2369/8058 `List-Unsubscribe`(+One-Click) headers when configured; header injection blocked on every value; log driver default so dev/CI never send real mail (`TestBuildSMTP*` unit-test the assembly). Digests now ship an auto-escaped `html/template` alternative (brand header + `<ul>` of the who/where lines) beside the text body. **One-click unsubscribe (P-20):** each digest carries `{base}/api/v1/unsubscribe?o&u&sig`, an HMAC-SHA256 over `unsub|org|user` (WEFT_SIGNING_SECRET, domain-separated from the files MAC, NO expiry — links must not rot); the endpoints self-authenticate OUTSIDE withAuth (the download precedent, no Authorization read) — GET renders a POST-only confirmation form and NEVER mutates (mail clients prefetch it), POST turns off every settable kind's email pref via one unnest upsert and is idempotent; a bad/forged/tampered sig is a constant-time 401, an unknown user under a valid sig is 404, and with no secret configured both verbs 404. Claim = FOR UPDATE SKIP LOCKED, mark-then-send (at-most-once, never duplicates), seen cancels pending email, digests carry who/where and NEVER message content. `TestNotificationEmails`, `TestEmailKeywordDefault`, `TestUnsubscribe` (forged-sig 401 load-bearing, red/green). Gaps: presence-aware offline check (unseen-after-delay is the proxy until cross-node presence); per-kind unsubscribe page (v1 is all-or-nothing — resubscribe via the prefs API); HTML polish is client-era; push medium |
| Alert words (N-1 kind 4) | **PROVEN (core)** | the last reserved reason class closes: per-user words (replace-set `GET/PUT /alert-words`, stored lowercase, 50×50 caps) matched case-insensitively on WORD boundaries (SQL substring prefilter over channel members' words → Go `\b` refine — "redeployment" never fires "deploy"); the specificity ladder holds (mention/followed beat keyword; keyword UPGRADES plain activity to kind 4 in the level pass); mute suppresses keywords (no mention-style breakthrough; unmuted-thread revive applies); edits that newly introduce a word ping once (NOT-EXISTS guard against any prior row for the message); DMs never keyword-ping (the DM row already does); keyword emails default ON (setting a word IS the opt-in) via the kind-4 pref now unreserved. `TestAlertWords`. Gaps: CJK/word-boundary edge cases (\b is ASCII-ish), no phrase highlighting in clients |
| Notifications | **PROVEN (first slice)** | ADR-011 pipeline step 1 for the two reasons whose data already rides every message event: direct mentions + DMs, materialized by a named txid-gated event-log consumer (NOTIFY-driven + sweep — the M0 Consumer's third user). Recipient must be able to SEE the message (private-channel mentions of non-members insert nothing); imported history never notifies (E4); at-least-once replays are no-ops via the dedupe key (cursor-reset replay tested); self-mentions skipped; mention beats dm for the same message. `GET /notifications` (inbox + unseen via the partial index), `POST /notifications/seen`; live best-effort notification.created ping to the user's connections — `TestNotificationPipeline`. **N-1 steps 2–3 live**: followed threads boost (kind 3) and override channel level; level=all yields activity pings (kind 5); the SEPARATE mute flag suppresses both while mentions/DMs break through; an unmuted thread revives activity inside a muted channel; more specific reasons win (one row per user+message) — `TestNotificationDepth`. Settings via `PUT /threads/{id}/subscription` (followed/muted/unmuted/clear; roots rejected per F-15) and `PUT /channels/{id}/notification` (own level + mute; not event-logged — personal prefs, read-state precedent). Replay note (documented in the runner): resolution reads CURRENT settings, so cursor resets re-evaluate history under today's preferences; the dedupe key absorbs redelivery, not settings changes. Gaps: keyword/wildcard/group-mention/item-event reasons, medium prefs (step 4 — the per-medium tri-state columns exist unused), DND quiet-hours schedule (step 5 — the snooze + VIP pierce now live, see the DND / VIP row; the weekly schedule stays dormant), privacy (step 6), read_at engagement, email/push media, client badge only (backend-first directive) |
| DND / VIP pierce (N-2) | **PROVEN (core)** | ADR-011 N-2's suppression layer, owned by the notification module because it gates delivery: `GET/PUT /api/v1/dnd` set the caller's snooze (`snoozed_until`; null clears, future-only, at most 30 days out — every rule 400-tested) and `GET/PUT /api/v1/vips` replace-set the priority-contact list (<=50, the caller's own id silently dropped, every id validated as a live org member in one count query → oracle-free 404 on any miss or foreign/cross-org id, a second PUT replaces the whole set). Enforcement suppresses DELIVERY, never the row (N-4: the badge accrues regardless): the live ping (`Runner.insert` → `dndSuppressed`, one indexed lookup only when a fan would fire) is withheld when the recipient is snoozed AND the message's actor is not on their VIP list; the email worker's claim query excludes those same rows and LEAVES them unmarked (`emailed_at` stays NULL), so the next sweep after the snooze lapses delivers them — a delay, never a drop. A VIP pierces both lanes. `TestDNDSuppression` proves the whole matrix with a capturing fake fanout + the mail capture: baseline (row + one ping + one email), snooze (row still lands, no ping, no email, row unmarked), lapse → the delayed email exactly once, VIP pierce (alice's DM pings and emails through the snooze while a non-VIP charlie stays suppressed), plus the validation 400s/404. Not event-logged (per-user settings, read-state precedent). Gaps: the `dnd_setting.schedule` JSONB (weekly quiet hours) stays DORMANT — it needs per-user timezones before it can be honored, so it is neither parsed nor exposed; the `dm_breakthrough` table now backs the breakthrough endpoint (see the DM breakthrough row); broadcast @here/@channel DND-respect (N-2) waits on the wildcard-mention reason class |
| DM breakthrough (N-2) | **PROVEN (core)** | ADR-011 N-2's "notify anyway": `POST /api/v1/dms/{id}/breakthrough` lets a DM sender spend ONE allowance per recipient per UTC day to re-fan a DND-suppressed live ping — the in-app row already landed (N-4), so this only re-delivers it over the fanout seam, bypassing the DND gate (the whole point). Oracle-free participant gate (nonexistent and non-participant both 404); 1:1 only (group → 400 "for direct conversations", self → 400); the recipient must currently be snoozed (else 409, use NOT burned) AND there must be an unseen kind-1 DM notification from this sender in THIS conversation (else 409, use NOT burned); the allowance is the `dm_breakthrough` PK (sender, recipient, UTC `used_on`) consumed with ON CONFLICT DO NOTHING → a second same-day call 409s while a new UTC day is a fresh row. No email side (the pending digest rides the normal post-snooze sweep) and no event (personal signal, DND/VIP precedent). `TestDMBreakthrough` (suppressed → re-fanned happy path, same-day 409, backdated next-day, not-snoozed/no-pending 409 with the use un-burned asserted, group/self 400, outsider 404). Gaps: the client "notify anyway" affordance is client work; a per-org disable knob is later |
| S3 driver & signed links (P-07) | **PROVEN (links)** / **UNEXERCISED (s3)** | `POST /api/v1/files/{id}/link` mints a 10-minute capability URL after running the SAME union ACL as a download (via the extracted `authorizeDownload`, which authorizes WITHOUT opening the blob); `GET /api/v1/files/{id}?sig=&exp=&org=` then serves the bytes with NO Authorization header (what makes `<img src>` work), verifying a hex HMAC-SHA256(secret, "file_id\|exp\|org_id") in constant time (`hmac.Equal`) with 30s leeway past exp. The signature IS the capability — the per-viewer ACL is skipped — but the row load stays org-scoped and live-only, so a link for the wrong org 404s (org-pinned: a leaked link cannot cross orgs) and a file GC'd since minting 404s. The download route is no longer `withAuth`-wrapped: the handler self-authenticates the bearer path (unchanged behavior, per-user limited) and leaves the signed path open; both stream attachment + nosniff (inline stays the avatar-only magic-validated path). Secret is `WEFT_SIGNING_SECRET` (`files.SetSigningSecret`, wired in weftd); unset → minting refuses with a clear 500. `TestSignedLinks` (mint→no-auth-fetch→bytes, tampered/expired 401, within-leeway 200, wrong-org 404, deleted 404, config-missing 500, bearer path intact). Reconciliation flagged: the URL carries `org` (the written spec's URL omitted it) because the HMAC's `org_id` AND the "foreign-org → 404" test BOTH require org as an independent org-scoping input — the secure reading, not an invented param. Gaps: no pre-expiry link revocation (short TTL is the mitigation); one server-wide secret, no rotation lane. **S3 driver** (`internal/platform/blob/s3.go`) implements the SAME `blob.Store` seam via aws-sdk-go-v2 (the one new dependency): PutObject (idempotent, content-addressed), Open=GetObject, Delete tolerating NoSuchKey; AWS or any S3-compatible endpoint (MinIO/R2/GCS-interop) via `WEFT_S3_ENDPOINT` + path-style, `WEFT_S3_PREFIX` namespace, credentials from the SDK default chain (never our config keys). It is config-picked in weftd's `openBlob` (operators swap backend by env alone); `blob.Open` stays the fs entry point so all 8 fs-backed suites are untouched and green. Its integration test `TestS3RoundTrip` SKIPS unless `TEST_S3_ENDPOINT` is set, so the S3 driver is compiled + seam-conformant but UNEXERCISED end-to-end here (gap: no MinIO service in CI) — the unchanged fs suite proves the seam contract |
| Files | **PROVEN (first slice)** | ADR-012 wakes behind the **blob seam** (`platform/blob.Store`, driver via config — fs/bare-metal implemented; S3/GCS/Azure = one file + a factory case, core untouched): streamed multipart upload with a 25 MiB cap, content-addressed keys (org-scoped sha256 fan-out → idempotent puts + org dedup proven), `file.uploaded` events. Download enforces **F-12 union-of-referencing-ACLs** per (viewer, reference): uploader always; otherwise at least one referencing message the viewer can see (three-way container rule), oracle-free 404 otherwise, forced `attachment` + `nosniff` headers (stored-XSS hygiene). Message links to `/api/v1/files/{id}` become references at write time via the messaging hook (sets has_attachment); **attaching someone else's unreferenced file silently creates nothing** (smuggle path tested) — `TestFilesBlobSeam`. Uploads now also run the P-19 scan seam + org quota (see that row). Gaps: no blob GC when references hit zero (retention lane), work-item references, inline image rendering (allowlist later), importer files lane |
| Upload scan seam + storage quota (P-19) | **PROVEN (core)** | wakes the dormant `file.scan_status` column (F-7) and adds a per-org byte quota. **Scan seam:** `files.Scanner` (Scan(ctx,name,mime,r)→Verdict) wired via `SetScanner` (the blob.Store/mail.Sender pattern); weftd wires NO scanner yet (no real driver — clamav/ICAP is a later one-file driver), so uploads stay scan_status 0 (pending), never a fake "clean". Upload scans the fully-spooled, size-checked bytes BEFORE storing: a scanner error fails CLOSED (500, no row, no blob); Clean records status 1; a Quarantine still stores the bytes + row (status 2 — evidence for compliance/holds) but returns 422 and is inert — the message attach hook skips scan_status 2 like a deleted file (NO reference forms, even for the uploader) and every byte-read path (authorizeDownload for bearer + signed-link MINT, OpenSigned, OpenAvatar) treats it as an oracle-free 404. Avatar/emoji set route through Upload, so a quarantined image 422s before the pointer is written. GC treats a quarantined row like any unclaimed file (no scan_status special-case). **Quota:** cap in `org.settings.storage_quota_bytes` (files owns this key via jsonb_set; 0/absent = unlimited); `PUT/GET /api/v1/admin/storage-quota` are manage_org-gated (a plain member 403s both), the set is event-logged `org.quota_changed`. Enforcement in Upload AND StoreDocument (exports count too): `SUM(size_bytes)` over live files (rides the new partial covering `file_org_live_idx`, migration 0014) + incoming > cap → 413, checked BEFORE storing so an over-cap upload leaves no blob/row. Row-accounting (a dedup-shared blob counts each row; a GC purge frees the bytes); inclusive boundary (used+incoming==cap passes). `TestUploadScan` (clean/quarantine/scanner-error/no-scanner, four read 404s, no-reference attach, avatar+emoji rejection, GC-normal) + `TestStorageQuota` (boundary, over-413 red/green, purge frees, max_bytes 0 unlimited, non-admin 403, event logged). Gaps: no real scanner driver (seam only); async re-scan lane; per-user quotas; blob-level dedup accounting; the 413/422 statuses were added to the apperr taxonomy (not reduced to 400) so the honest status ships |
| Avatars & custom emoji (P-06) | **PROVEN (core)** | `PUT/DELETE /api/v1/me/avatar` set/clear the caller's avatar and `GET /api/v1/users/{id}/avatar` streams it. Uploads are size-gated (≤2 MiB) and image-validated BY MAGIC BYTES (http.DetectContentType allowlist png/jpeg/webp/gif) in the transport BEFORE the content-addressed store ever sees the bytes — the client mime is never trusted, and SVG is rejected as an XSS vector (proven: renamed text, `<?xml><svg>`, and oversize all 400). Bytes ride the normal `files.Upload` path; identity owns the `user_account.avatar_file_id` write (LLD), and the FK keeps GC off the file while it is pointed to. The read (`files.OpenAvatar`) is org-public — any member fetches any member's avatar; a guest sees only self or a live channel-mate (identity's `guestVisibleClause` mirrored as an EXISTS) — served with the stored mime, `inline` disposition (SAFE: magic-validated at upload), nosniff, `Cache-Control: private, max-age=3600`; a missing avatar or a guest-invisible target is an oracle-free 404, and a deactivated user's avatar keeps serving. Directory + batch profiles carry `avatar_file_id` so clients build the URL. Clearing the pointer makes the file GC-eligible after the unclaimed grace — proven end-to-end by backdate+sweep (pinned survives, cleared purged with its blob). `TestAvatars`. **Custom emoji**: `POST /api/v1/emoji?name=` (multipart, manage_org-gated, ≤256 KiB, the same magic-byte image gate) registers a name matching `[a-z0-9_]{2,32}` that is unique per org — reserved even after a soft delete (Zulip parity → 409); `GET /api/v1/emoji` lists live ones (name + file_id), `DELETE /api/v1/emoji/{name}` soft-deletes (`deactivated_at`); both write events (`emoji.created`/`emoji.deleted`, the appended `EntityEmoji`), and the `custom_emoji.file_id` FK keeps the image off GC. Reacting with a custom name already works (reactions store tokens) — the registry only resolves name→file — proven surfacing in the message aggregates. `TestCustomEmoji`. Gaps: no image resizing/thumbnails v1; no avatar-change events (profiles refresh on next fetch); no org-wide emoji IMAGE-serving path (the list returns file_id; the union-ACL download still gates the bytes — an OpenAvatar-style public read is deferred); no per-org "who may add emoji" knob (manage_org only) |
| Compliance (ADR-013) | **PROVEN (first slice)** | retention policies (AD-3: org default + channel override, duration + keep_edits, upsert per scope) and legal holds (AD-4: custodian and/or channel scope, create/list/release, released rows kept as audit record) via `/api/v1/admin/*` — every operation gated on `compliance_officer`, which is NEVER seeded (F-9): `TestComplianceAdmin` proves even the org owner is 403'd until the verb is explicitly granted, and all acts are event-logged. Blob GC live (`compliance.Janitor`, hourly): the F-1 rule enforced end-to-end — unclaimed uploads (zero references, 35-day grace; Zulip parity) and dead-reference files (every referencing message deleted, 30-day restore window) are soft-deleted with bytes reclaimed through the blob seam ONLY when no live row shares the content-addressed key (org dedup twin rule); active holds block both lanes (custodian and channel scope) and purge resumes on release; every purge is a `file.purged` system event; per-file lock + full predicate recheck inside the tx closes the scan/attach race; liveness is conservative (unevaluable reference types + dangling refs count as live). `TestBlobGC`. Revision scrub live (same janitor): where the EFFECTIVE policy (channel override → org default, nearest wins; DM/space messages fall to org until their rungs land) says keep_edits=false, kind-1/2 revisions lose prev_source/prev_ast while the structural record (who/when) survives; kind-4 deletion capture never touched (that's the message-retention lane's evidence); custodian/channel holds freeze originals until release; per-org `retention.revisions_scrubbed` events carry counts — `TestRevisionScrub` covers the whole ladder. Compliance export live (AD-5, API-first): `POST/GET /admin/exports` queue scoped jobs (users=authored-by, channels, spaces, date window; empty = full org, all validated org-local) run by a SKIP LOCKED worker (nudge + tick, 15-min crash reclaim) into a downloadable JSON file — INCLUDING private channels, DMs, edit history, and deleted-message tombstones (what Zulip's CLI export can't carry); retention-purged revisions surface as null BY DESIGN; attachment evidence is PINNED via EntityExportJob file_references so GC keeps the bytes after the source messages die (proven: dead-ref sweep spares the pinned file) — closes PR #42's open question. `TestComplianceExport`. **Export byte-bundles (P-32):** `POST /admin/exports` scope gains `include_files` (default false → today's bare JSON, byte-unchanged). When true the worker streams an `export-<id>.zip` (application/zip) INSTEAD: `export.json` (byte-content identical to the plain export of the same scope), `manifest.json` ({job_id, generated_at, file_count, total_bytes, missing:[ids]}), and `files/<file_id>_<name>` — one deduped entry per attachment PINNED by this job (the #45 EntityExportJob set), each streamed blob→zip via `files.OpenForBundle` (org-scoped, NOT the union ACL — the export is already officer-authorized). Streaming is end-to-end `zip.Writer → io.Pipe → files.StoreDocumentStream` (the new no-cap streaming store; `StoreDocument` is now a bytes wrapper over it), so memory is O(one zip block); a write error on either side cancels both via the pipe. A file whose blob is missing/unreadable at bundle time is SKIPPED and listed under `manifest.missing`, never fatal — so a job whose source messages died since pinning still bundles the surviving pinned bytes while a truly-purged blob degrades visibly (`TestExportBundle`, red/green: making that path fail the job is caught). The zip result rides the same result_file_id + officer-gated download lane as the JSON result (uploader = the officer; a non-officer 404s). Bundles are quota-enforced like any org storage: P-19's checkQuota was wired into StoreDocumentStream at serial-merge, and an export requested under a full quota FAILS (status 4, no result — TestExportBundle pins it). Gaps: eDiscovery/partner manifest FORMAT (this is the raw bundle); zip64 handles >4 GiB bundles (archive/zip switches automatically); no incremental/delta exports. **Audit read API (P-31):** `GET /api/v1/audit/events` streams the org's raw event log to a `compliance_officer` — F-9 gated so even the owner 403s without the explicit grant (`TestAuditReadAPI`, red/green on dropping Require) — newest-first and keyset-paginated (`next_cursor` = last id on a FULL page, else 0). Optional AND-composed filters (entity_type, verb ≤64 chars, actor_id, entity_id, since/until on occurred_at — RFC3339, since inclusive / until exclusive per the search precedent, malformed → 400 — cursor, limit 1..200 default 50, clamped not errored) build a parameterized WHERE via the search add() placeholder pattern, never interpolated. payload/origin ride through verbatim as raw JSON; hint is excluded (routing noise); strictly org-scoped (two orgs, each officer sees only their own). No txid gate (a historical read, not a consumer — a late-committing row may surface on a later page) and no new index: pages ride (org_id, id DESC), so sparse filters over a huge org can scan long (a covering (org_id, verb, id) index is the queued mitigation). Gaps: byte access for non-uploader officers (an export result downloads only for the requesting officer); export deletion/pin release; workspace/space/dm policy rungs and space/query hold scopes rejected until their consumers exist; per-entity audit convenience route + workspace-scoped officer reads later; no audit-read client UI (the API is live); orphan-blob audit tool |
| Message retention vacuum (P-17) | **PROVEN (core)** | The enforcement lane for `retention_policy.duration_days` (CRUD since #62, no consumer until now) — two `compliance.Janitor` lanes on the hourly sweep. **Vacuum**: a message whose age has passed its EFFECTIVE policy — nearest-wins channel(3)→org(1), the scrubRevisions ladder; the COALESCE default is -1 = forever = never vacuumed, so a message with no policy or a forever policy is untouched — is tombstoned via `deleted_at` (every product read path, INCLUDING the P-16 authed + anonymous surfaces, already filters it, so it leaves the product at once) plus a new nullable `retention_vacuumed_at` marker. Content is left in place (hidden, NOT blanked): a vacuumed message in its window still physically exists for compliance export and recovery, which also makes restore a trivial timestamp clear and sidesteps `message_revision.edited_by` being NOT NULL for a system actor (no capture row needed). Pins dropped + thread count decremented (the DeleteMessage side-effects that keep the pin cap and counters honest); a `retention.message_vacuumed` system event per row. **Purge**: after the restore window (`VacuumRestoreWindow`, default 30d, `GC_VACUUM_RESTORE_DAYS`; mirrors the file DeadRefWindow) the row is permanently removed — reaction/pin/saved_item/message_user_flag/message_report/message_link_preview/message_revision/reminder rows DELETEd, the sent `scheduled_message` record DELETEd (NULLing `sent_message_id` would re-arm the due partial index — a re-send bug avoided), `thread.root_message_id` + `forwarded_from_message_id` NULLed (no FK, hygiene), then the message row DELETEd; a `retention.message_purged` event. `event_log` has NO FK to message, so the audit trail (created + vacuumed + purged) SURVIVES the purge — asserted. Both lanes use the file lanes' batch-scan + per-row lock + in-tx recheck (a hold or policy change between scan and lock spares the row), and both carry the legal-hold guard (custodian OR channel): a hold freezes the vacuum, and a hold placed DURING the window freezes the purge, resuming on release. `TestRetentionVacuum` covers the age boundary, the nearest-wins ladder, tombstone correctness (invisible on the P-16 paths, content retained, event logged), the FK cleanup, and audit-spine survival; the vacuum's hold guard and the purge's restore-window bound are BOTH red/green-proven (neutering either fails the test at the load-bearing line). Gaps: first-class restore API (recovery is DB-level within the window — a re-render lives in messaging); workspace/space/dm policy rungs; a partial `(retention_vacuumed_at)` index if sweep latency shows; attachment blobs still reclaim through the existing file dead-ref lane once the message tombstone lands (documented linkage) |
| Automations (ADR-014) | **PROVEN (CRUD)** | scope-owned rules (AU-2: org rules gated manage_org, channel rules administer_channel — never a creator's personal property) in the AU-3 canonical definition format (v1 vocabulary: message.created/workitem.created/workitem.status_changed triggers, post_message steps; unknown fields rejected; channel-scope rules cannot post outside their channel). Created DISABLED. The F-13 consent arc is enforced and proven: acting as another human requires THEIR consent (creator naming self = instant), a definition edit bumps the version, CLEARS consent and DISABLES the rule, and only the named actor can re-consent (`TestAutomationCRUD`). Soft delete; every act event-logged. Runner live (`automation.Runner`, the "automations" consumer — NOTIFY-driven + sweep, txid-gated): AU-1 proven — message.created AND workitem.created drive rules through one trigger vocabulary (`TestAutomationRunner` has a Jira-style item trigger posting a chat announcement). AU-4 guardrails all proven: runs keyed (automation, event) — a full cursor-reset replay re-fires NOTHING; self-loop hard block; rule→rule chains only through allow_rule_trigger opt-in; chain depth capped at 3 via event-hint depth counting, with the over-cap run recorded THROTTLED (status 6), never silently dropped. Steps execute atomically with the run row (savepoint per step: a failing step's trace commits while its writes roll back — archived-channel failure proven visible as status 5). Automations author as a lazily-created per-org agent principal (or the CONSENTED human actor); their message events carry ActorAutomation + the rule id. **Failure notifications (P-25):** a run ENTERING the failing state (status 4 partial-error or 5 failed) records a kind-6 in-app notification for the rule's write-gate holders — `perms.HoldersAt(manage_org, org)` for org rules, `HoldersAt(administer_channel, channel)` for channel rules, so whoever may edit the rule hears it broke (agents + deactivated accounts excluded via the closure) — written in the finish tx and live-pinged after commit (DND-gated via the shared `dndSuppressed`, system actor so no VIP pierce; the badge row lands regardless). Throttled to at most hourly per rule while it keeps failing (an in-tx probe of the prior terminal run riding `automation_run_list_idx`), re-armed by any success; the `(user, kind, entity_type=18, run)` dedupe key caps it at one row per admin per failed run. kind-6 email defaults OFF (in-app doorbell — the detail lives in ListRuns; admins opt in via the prefs API). `TestAutomationFailureNotifies` (both org admins alerted / non-admins + guests get none — red/green; immediate re-failure throttled; backdated re-alert; success re-arm; a channel-scope rule alerts its channel admins NOT the org admins; a snoozed admin's ping suppressed while the row still lands; email off-by-default then sent after opt-in) + `TestHoldersAt`. Gaps: conditions + templating now live (see the Conditions + templating (P-22) row); schedules/webhooks/LLM steps, per-rule failure-alert mute knob, workspace/space scope rungs (and their failure-alert holders) when those admin verbs land, budgets unenforced, per-org queue isolation at fleet scale |
| Conditions + templating (P-22) | **PROVEN (core)** | ZERO migrations — `automation.definition` JSONB gains `conditions` + `{{event.path}}` spans; every rule stored before P-22 (no `conditions` key, no spans) validates and runs byte-identical, DisallowUnknownFields still on. **Conditions** (`conditions.go`): `Definition.Conditions` optional, ≤10, ANDed; path grammar `event.` + 1..5 dot segments of `[a-z0-9_]+` (shared with templating); ops `eq`/`ne` (number\|string\|bool, same-JSON-type), `gt`/`lt`/`gte`/`lte` (numbers), `contains` (substring), `in` (1..20 same-type scalars), `exists`/`not_exists` (value must be ABSENT). validateDefinition 400s bad path, unknown op, and type-invalid operands. **STRICT typing, no coercion** — `"42"` never equals `42`; the payload is decoded ONCE per evaluation with `json.Decoder.UseNumber()` so integer `eq`/`ne`/`in` compare the canonical `json.Number` string (BIGINT ids past 2^53 stay exact — `9007199254740993 ≠ …992` proven), non-integers via Float64; a present-but-null value counts as ABSENT. Conditions evaluate INSIDE `match()` before any DB work, so a miss creates **NO run row** (asserted: a non-matching channel's message writes zero rows). **Templating** (`template.go`): `{{event.path}}` in `post_message` Content only, ≤20 spans, validate 400s any span that is not a valid path; at execute the UseNumber payload resolves each span — string/`json.Number` verbatim, bool `true`/`false`, missing/null → `""`, object/array **fails** the step ("resolves to a non-scalar"); post-expansion length outside 1..4000 fails (never truncates); text outside valid spans untouched. **Mention-injection guard (load-bearing, STRUCTURAL not escaping):** (1) validate 400 when any mention node's label in the literal content contains a span (no templating inside `@**…**`); (2) at execute, parse literal vs expanded content with a no-op resolver and require mention-label **MULTISET** equality — any drift fails ("template expansion may not alter mentions"). Multiset (not node count) defeats the net-zero backslash-suppression attack; the literal parse is cached once per rule load. Formatting injection (bold/code from a value) is cosmetic and accepted. `TestAutomationConditions` + `TestAutomationTemplating` (real Postgres, e2e through the runner: eq gates one channel not another with the non-matching event leaving no run row, `in`/`exists`, a `workitem.created` rule rendering the real `{{event.key}}`, non-scalar + overflow as failed traces, and a synthetic `eventlog.Append` whose payload smuggles a member's `@**Name**` → step fails, no post, no mention notification) + unit tables (`TestParsePath`, `TestMatchConditions`, `TestValidateConditions`, `TestFindSpans`, `TestValidateStepContent`, `TestExpandTemplate`, `TestRenderStepMentionGuard`). RED/GREEN (pinned in test comments): neuter the label-multiset check → the smuggled mention posts, `doc.Mentions()` fans out, and `bob mention notifications == 0` goes red; allow `eq` coercion → `"N"` matches `N` and the strict-typing `strict-fire == 0` goes red. Gaps: OR/if-else condition groups; query conditions (ADR-010 DSL); user/group conditions; for-each + `{{issue}}`-style rebinding (AU-1); dry-run + expression preview (AU-2, the recorded answer to "why didn't it fire"); template filters / `{{json …}}`; mention-a-variable-user as an id-typed step field; templating in step kinds beyond post_message |
| Trigger kinds: schedules, webhooks, slash (P-23) | **PROVEN (core)** | Migration 0017 adds `schedule_next_at` + `webhook_token` to automation plus the partial claim index `(schedule_next_at) WHERE schedule_next_at IS NOT NULL AND enabled AND deleted_at IS NULL`. The trigger KIND lives in the JSONB definition, so absent = `event` and every rule stored before P-23 validates and runs byte-identical. `validateTrigger` enforces each kind's shape and rejects fields foreign to it (a stray verb on a webhook rule 400s); the three internal verbs (`automation.schedule_due`/`webhook_received`/`slash_invoked`) NEVER enter `triggerVerbs`, so matching them is TARGETED — schedule/webhook by `payload.automation_id == rl.ID` (channel-scope filter skipped), slash by command equality with the existing channel-scope filter expressing "org scope any channel, channel scope only its own". P-22 conditions + templating apply to ALL kinds (`{{event.body.x}}`, `{{event.text}}` proven to flow). **Schedules**: structural grammar (`minutes` n≥5 / `hour` minute 0..59 / `day` at HH:MM / `week` on mon..sun at HH:MM, optional IANA `tz` via `time.LoadLocation`, default UTC — no cron dep); `nextFire(sched, now, loc)` is a pure fn, `TestNextFire` pins all four grammars, a spring-forward DST gap normalizing FORWARD (nonexistent 02:30 → 03:30, never back to 01:30) and week rollover, `TestValidateSchedule` the n<5 / bad-tz / bad-HH:MM / bad-weekday 400s. `schedule_next_at` lifecycle in Update: computed on enable, recomputed on a definition change that stays enabled, NULLed on disable or a non-schedule trigger; Create (disabled) never sets it; the F-13 arc composes (a definition edit disabling a human-actor rule NULLs the fire time). Scheduler lane (`RunDueSchedules`, a 30s goroutine beside sweep): one tx locks ≤100 due rows `FOR UPDATE SKIP LOCKED`, advances each to the next fire computed FROM NOW (a slot missed in downtime fires ONCE on recovery, no burst catch-up), and appends `automation.schedule_due` (`ActorSystem`, EntityAutomation) — all atomic; the consumer's `(automation, event)` key absorbs redelivery. **Webhooks**: `POST /api/v1/hooks/rules/{id}/{token}` UNAUTHENTICATED outside withAuth, behind the per-IP authLimit AND a per-rule limiter (~1 rps burst 10 → 429, the echo-loop bound); absent id / wrong token / disabled / non-webhook rule are ONE indistinguishable 404, token compared with `subtle.ConstantTimeCompare` over sha256s; body ≤64KB + `json.Valid` checked ONLY after auth (413/400); a pass appends `automation.webhook_received` (`ActorSystem`, payload `{automation_id, body}`) → 202, retries create distinct runs. The 32-byte crypto/rand hex `webhook_token` is minted when the trigger becomes a webhook and NULLed otherwise, surfaced only to scope admins (Create + List), rotated via scope-admin `POST /api/v1/automations/{id}/webhook-token`; the token never enters an event payload or log line. **Slash**: authed `POST /api/v1/automations/slash {command, channel_id, text?}` gated by the exported `messaging.RequireChannelSend` (send_message + membership + live channel — the prep-commit lift, NO duplicated access control), text ≤2000 runes with control chars stripped (P-29 precedent), appends `automation.slash_invoked` (`ActorHuman`, payload `{command, channel_id, user_id, text}`) → 202; multiple rules may fire one command (OQ-AU12). `TestAutomationSchedules` / `TestAutomationWebhooks` / `TestAutomationSlash` (real Postgres, end to end through the runner) + the two unit tables. RED/GREEN (pinned in comments): (1) neuter the webhook token compare (accept any token) → the wrong-token 404 becomes a 202, red at `automation_webhooks_test.go:149` — the load-bearing line; (2) drop the membership check from the shared send gate → the non-member slash 403 becomes a 202, red at `automation_slash_test.go:160`. Gaps: cron-string grammar; webhook sender dedupe key (at-least-once today); Slack-payload-compatible inbound posting (that's P-27); slash discovery/autocomplete + command namespace collisions (OQ-AU12); per-org scheduler sharding at fleet scale; catch-up policy knobs; sub-hour DST zones a documented non-goal |
| Importers (founding requirement) | **PROVEN (Zulip, deepening)** | `weftd import-zulip`: topics → titled Threads, ORIGINAL timestamps end-to-end (entities + event-log occurred_at/recorded_at split — E3 proven), provenance-idempotent re-runs (D5), email-match or claimable placeholders (D4), mention re-resolution, our-engine re-render, dry-run fidelity report with named losses + visible collision renames. **Roles carry onto created placeholders** (existing users never elevated/demoted — skips counted); **named user groups** import with membership + nesting (system groups map onto the seeded role groups, fullmembers coarsens to members, closure rebuilt in-tx); **read watermarks** from usermessage read flags (monotone upsert; sparse unread below the watermark coarsened + counted — DM threads included); **DMs import** (legacy personal AND modern direct-message-group recipients; canonical key matches the native dm module so imported + native history share one conversation; a conversation with any bot participant is skipped WHOLE and counted — dropping the bot would shrink the key onto a different real conversation) — `TestZulipImportShowcase`, dry-run/write parity asserted. **Edit history imports** (attributed content edits → kind-1 revisions re-parsed through our engine, backdated edited_at; null-editor pre-2017 entries skipped + counted — attribution never invented); **attachments import through the blob seam** (bytes from the export's uploads/ tree, content-addressed via files.StorageKey so they dedup with live uploads; /user_uploads links rewritten to managed URLs; m2m references + has_attachment; missing bytes counted). Gaps: avatar/emoji files, per-user sub prefs (mute/pin) and starred/mentioned flags, tarball unpack, per-chunk streaming for multi-GB exports, Slack/Jira adapters |
| Load generator | **PROVEN** | `cmd/loadgen` + `internal/loadtest`: many-tenant HTTP+WS load, id-correlated delivery latency, histogram percentiles. Unit-tested (`histogram_test.go`); used for the numbers below |
| Scale — code path | **MEASURED** | uncontended full send = ~3.8 ms (auth+perms+AST+insert+event+notify+commit). Stable under 150 tenants / 450 concurrent WS with **zero errors**. See docs/PERF.md |
| Scale — per-node capacity | **NOT YET MEASURED (rig-bound)** | laptop rig conflates loadgen+server+Docker-VM Postgres on 8 cores (VM at 228% CPU was the ceiling, not weftd at 63%). A true per-cell number needs dedicated hosts (PERF.md has the procedure). No CI perf floor yet — would be environment-bound on the shared runner |
| Web client (dogfood) | **WORKS-THIN** | zero-build embedded client (`internal/webui`, one HTML file, brand injected from token): auth, channel list/create/join, threads (create/open/back), rich message send with live WS render (bold/code/mention/emoji verified in a real browser via Playwright), typing + unread badges + mark-read, **spaces + kanban board** (columns from the space's workflow, drag-and-drop status transitions with the derived-resolution ✓ flipping live, live board refresh from `workitem.*` events, item create with description-as-root-message, item view = the thread with a comment composer, thread → item promote button) — all browser-verified incl. 480px board overflow-scroll. Real author names everywhere via cached batch profile resolution (messages incl. live appends, search result meta, typing indicator by name, own identity in the sidebar; initials avatars; deactivated authors labelled). Composer @-mention autocomplete: directory-backed with presence dots, substring filtering (spaces allowed mid-query), arrow/Enter/Tab/Esc keys and mouse, inserting canonical @**Full Name** syntax at the caret. Direct messages: sidebar section with unread badges, other-participant names, and live presence dots (also in the picker), a directory-backed picker (1:1 and group), the conversation as a plain chat view with live appends, mark-read, and DM-scoped typing indicators by name. Item search hits show their key in the result meta and click through to the item's thread view. Scrollback: nearing the top of any chat view loads the previous page (server before-id cursor), keeps the viewport anchored, and marks the beginning of the conversation when history is exhausted. Handler unit-tested; the flows were browser-verified, not automated in CI. Reaction chips under every message: aggregates with mine-highlight, click-to-toggle, hover +👍 quick-add, live updates from reaction.* events (browser-verified incl. a second user's reaction arriving over WS). Gaps: no reconnect UX polish, no board reordering within a column — this is the dogfood UI, not the real client app |

## Standing rules
1. A subsystem may not be marked done anywhere (README, PRs, timeline) at a
   rating higher than this ledger's.
2. Every PR that touches a subsystem updates its row — reviewers check the row
   against the diff.
3. "DESIGNED" is never evidence. Tests against real infrastructure are.

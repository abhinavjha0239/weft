# Reality ledger — what's proven vs what's scaffolding

*The anti-self-deception document. Every subsystem is rated honestly; a claim
without a cited test is not "done." Update this in the SAME PR that changes a
subsystem's reality. Ratings:*

- **PROVEN** — behavior demonstrated by a cited test against real Postgres
- **WORKS-THIN** — runs end-to-end, known gaps listed
- **PLACEHOLDER** — deliberately fake stand-in; replacement designed
- **DORMANT** — schema/design exists, zero code paths exercise it

| Subsystem | Status | Evidence / gaps |
|---|---|---|
| Event log write+consume | **PROVEN** | `TestCommitOrderGate` (commit-order race defused), `TestCursorAdvance` (monotone cursors, backdated imports) |
| Gateway resume (F-2) | **PROVEN** | `TestMessageEndToEnd`: disconnect → send-while-away → reconnect(last_id) → gap replays; checkpoint heartbeats |
| Ephemeral signals (typing, read-sync, presence) | **PROVEN** | client→server WS frames + in-memory fan-out, never event-logged (ADR-002 P5): typing scoped to channels OR DM conversations (sender gated by their OWN membership view — a non-participant's frame is dropped; targets by theirs), excluding all the sender's devices; read_marker → durable MarkRead + readstate.synced to the sender's OTHER devices only; **presence derived from the connection registry** — first connection broadcasts online, only the LAST disconnect broadcasts offline (multi-device proven), REST snapshot at `GET /presence` then live presence.changed. `TestEphemeralSignals` + `TestDMTypingAndPresence` (race-clean). Gaps: presence is per-gateway-process (single-node slice — shared plane arrives with multi-node cells), no idle/away state, typing has no server-side auto-expiry (clients time out stale indicators) |
| Outbox rule (write+event, one tx) | **PROVEN** | send path in `server.sendMessage`, asserted via live delivery in e2e |
| Schema (all 10 migrations) | **PROVEN to apply** / **DORMANT in bulk** | applies clean on PG17 + constraint smoke tests; **migrations are embedded in the binary** (`migrations.FS`) so `weftd migrate`/`serve`/`import-zulip` are working-directory-independent, an empty set is a build/runtime error (never a silent no-op), and every DB test package runs the production runner. Automations, files, notifications, compliance tables still have ZERO code exercising them — shape bugs there are undiscovered by definition |
| Auth | WORKS-THIN | bcrypt + hashed bearer sessions, 401 negative tested; **login brute-force rate-limited (PROVEN: `TestAuthRateLimit`)**; profile lookups: `GET /me` (boot identity) + batch `GET /users?ids=` (org-pinned, foreign ids silently absent, deactivated included+flagged, 100-id cap) — `TestUserProfiles`; bootstrap 409s only on a REAL slug collision — infra failures are 500 (`TestBootstrapErrorMapping`, red/green). Gaps: session revocation endpoint, 2FA (M4+), no profile edit endpoint |
| Transport quality (errors, limits, middleware) | **PROVEN** | error taxonomy via one mapper (`TestErrorTaxonomy`), per-IP pre-auth + per-user API limiters with bounded memory (janitor), request-id/logging/panic-recovery middleware, 1 MiB body caps |
| Permission model | **PROVEN (core)** | (verb,scope)→group resolver with closure lookup, live in the send path. `TestResolverCore` (nested-group transitivity, deny-by-default, allow-after-join), `TestMostSpecificWins` (channel override beats org default). Gaps: ~7 of ~40 verbs registered; profiles/custom groups have no API; grant∩permission for agents (CC-6) is a hook; read path (Get) still membership-join; item-scope VisibilityScope unenforced |
| Message rendering | **PROVEN (core)** | Portable AST engine live in the send path: CommonMark+GFM (tables/strike/tasklists/autolinks), chat hard-breaks, typed @**name** mentions resolved in-tx (ids in render + event payload), :emoji:, XSS-safe by construction (no raw HTML path; scheme allowlist) — `TestCommonMarkAndGFM`, `TestXSSNeutralized`, `TestMentions`, `TestRichContentEndToEnd`. Gaps: full emoji data table (20 starter codes), spoiler/math/quote-reply syntax, ADF/mrkdwn import converters (importer milestone), re-render-on-version-bump job |
| Gateway ACL filter | WORKS-THIN | membership-set filter + refresh on membership events. Missing: history_mode/protected floor, VisibilityScope, the client-side snapshot-refetch rule (no client exists) |
| Channels (CRUD) | **PROVEN (core)** | create (public/private, root thread, creator joins, name-collision 409), discoverability listing (private hidden from non-members), self-join public / 403 private, leave-revokes + re-join (history_from preserved — row reactivation), member.joined/.left events — `TestChannelCRUD`. Gaps: archive/rename/convert endpoints, invitations to private channels, per-channel admin (Channel Manager verb), public/private create-verb split, workspace picker UX for multi-workspace orgs |
| Direct messages | **PROVEN (core)** | dm_space container (ADR-008): create-or-get by canonical participant key (no locks — the unique index arbitrates), 1:1/group/self kinds, root thread per conversation, `POST /dms` + `GET /dms` (recency-ordered, LATERAL last-message probe), unread counts + mark-read on the same watermark model, live gateway fan-out to participants only (dm.opened decides from its participant list so the invited side hears it before its view refreshes). **Participation IS the permission**, and the inverse is enforced everywhere: a non-participant org member gets no fan-out, 404 on fetch, 403 on list/send, zero search hits — `TestDirectMessages`. Zulip DM import lands in the same containers (canonical-key interop); DM typing + presence live on the signal plane. Gaps: no leave/hide conversation, no per-conversation naming for groups |
| Threads | **PROVEN (core)** | create (titled/untitled, root message in one tx), retitle, resolve/reopen (idempotent), keyset-paginated list (root excluded), thread messages with before-cursor; F-15 root protections and the create_thread/edit_thread_title/resolve_threads verbs proven end-to-end (`TestThreadLifecycle`, incl. channel-scope verb override). Gaps: move/propagate between channels, DM/Space-governed threads, thread subscriptions (follow/mute), thread events not yet consumed by any UI |
| Read state | **PROVEN (durable core)** | F-7 watermark live: monotone clamped mark-read (`POST /threads/{id}/read`), per-channel unreads excluding own messages (`GET /unreads`) — `TestReadWatermarks`. Deliberately NOT event-logged (highest-volume action; scale contract). Gaps: per-thread unread breakdown, mention badges, F-17 materialized counters at scale |
| Search (messages, v1) | **PROVEN (core)** | `GET /search`: Postgres FTS over a GENERATED search_tsv column (native + imported messages auto-indexed), ADR-010 operator subset (from:/in:/has:link/is:resolved/before:/after: + quoted phrases), read ACL matching message fetch (channel membership; DM messages participant-only; **item descriptions/comments org-visible and searchable**, results tagged with item_key/space_id — promoted channel threads carry both scopes), ts_rank ordering + plain-text highlighted snippets; parser unit-tested + `TestSearch` (FTS, operators, ACL isolation, item-hit enrichment, empty-query 400). Client search box browser-verified. Gaps: item TITLES not searched (messages only), is:resolved reflects thread resolution not item status, full JQL-style work-item search, was:/changed: temporal (event-log), pgvector semantic lane, public-non-member visibility, cross-entity (files/people/channels) |
| Work items (the fusion) | **PROVEN (core)** | Spaces (seeded To Do/In Progress/Done + Task/Bug/Epic types + rank context), items owning space-governed threads (description = root message, one content system), **thread→item promotion keeping the SAME thread with unchanged channel ACL (D2 + F-5)**, comment-on-item = reply-in-thread via `POST /threads/{id}/messages`, **resolution DERIVED from status category incl. clearing on reopen (W-3)**, cross-space status ids rejected, per-space advisory-locked key_no, keys never reused (409) — `TestWorkItemFusion`. Message fetch-by-id shares the ListMessages visibility rule (space-thread messages org-visible), so live item comments render in the client. Gaps: org-wide space visibility slice (no VisibilityScope/profiles yet), no board reordering (rank is static v1), no epics hierarchy/links/sprints endpoints, promotion consent ceremony for broader-visibility Spaces not yet surfaced (all spaces org-visible so no exposure delta yet) |
| Notifications | **PROVEN (first slice)** | ADR-011 pipeline step 1 for the two reasons whose data already rides every message event: direct mentions + DMs, materialized by a named txid-gated event-log consumer (NOTIFY-driven + sweep — the M0 Consumer's third user). Recipient must be able to SEE the message (private-channel mentions of non-members insert nothing); imported history never notifies (E4); at-least-once replays are no-ops via the dedupe key (cursor-reset replay tested); self-mentions skipped; mention beats dm for the same message. `GET /notifications` (inbox + unseen via the partial index), `POST /notifications/seen`; live best-effort notification.created ping to the user's connections — `TestNotificationPipeline`. Gaps: muting/medium-pref/scheme steps of N-1 (steps 2–4), followed-thread/keyword/wildcard/item-event reasons, read_at engagement, email/push media, client has only a count badge (backend-first directive) |
| Files / automations / compliance | DORMANT | schema + ADRs only |
| Importers (founding requirement) | **PROVEN (Zulip, deepening)** | `weftd import-zulip`: topics → titled Threads, ORIGINAL timestamps end-to-end (entities + event-log occurred_at/recorded_at split — E3 proven), provenance-idempotent re-runs (D5), email-match or claimable placeholders (D4), mention re-resolution, our-engine re-render, dry-run fidelity report with named losses + visible collision renames. **Roles carry onto created placeholders** (existing users never elevated/demoted — skips counted); **named user groups** import with membership + nesting (system groups map onto the seeded role groups, fullmembers coarsens to members, closure rebuilt in-tx); **read watermarks** from usermessage read flags (monotone upsert; sparse unread below the watermark coarsened + counted — DM threads included); **DMs import** (legacy personal AND modern direct-message-group recipients; canonical key matches the native dm module so imported + native history share one conversation; a conversation with any bot participant is skipped WHOLE and counted — dropping the bot would shrink the key onto a different real conversation) — `TestZulipImportShowcase`, dry-run/write parity asserted. Gaps: edit history, attachment/avatar/emoji files, per-user sub prefs (mute/pin) and starred/mentioned flags, tarball unpack, per-chunk streaming for multi-GB exports, Slack/Jira adapters |
| Load generator | **PROVEN** | `cmd/loadgen` + `internal/loadtest`: many-tenant HTTP+WS load, id-correlated delivery latency, histogram percentiles. Unit-tested (`histogram_test.go`); used for the numbers below |
| Scale — code path | **MEASURED** | uncontended full send = ~3.8 ms (auth+perms+AST+insert+event+notify+commit). Stable under 150 tenants / 450 concurrent WS with **zero errors**. See docs/PERF.md |
| Scale — per-node capacity | **NOT YET MEASURED (rig-bound)** | laptop rig conflates loadgen+server+Docker-VM Postgres on 8 cores (VM at 228% CPU was the ceiling, not weftd at 63%). A true per-cell number needs dedicated hosts (PERF.md has the procedure). No CI perf floor yet — would be environment-bound on the shared runner |
| Web client (dogfood) | **WORKS-THIN** | zero-build embedded client (`internal/webui`, one HTML file, brand injected from token): auth, channel list/create/join, threads (create/open/back), rich message send with live WS render (bold/code/mention/emoji verified in a real browser via Playwright), typing + unread badges + mark-read, **spaces + kanban board** (columns from the space's workflow, drag-and-drop status transitions with the derived-resolution ✓ flipping live, live board refresh from `workitem.*` events, item create with description-as-root-message, item view = the thread with a comment composer, thread → item promote button) — all browser-verified incl. 480px board overflow-scroll. Real author names everywhere via cached batch profile resolution (messages incl. live appends, search result meta, typing indicator by name, own identity in the sidebar; initials avatars; deactivated authors labelled). Composer @-mention autocomplete: directory-backed with presence dots, substring filtering (spaces allowed mid-query), arrow/Enter/Tab/Esc keys and mouse, inserting canonical @**Full Name** syntax at the caret. Direct messages: sidebar section with unread badges, other-participant names, and live presence dots (also in the picker), a directory-backed picker (1:1 and group), the conversation as a plain chat view with live appends, mark-read, and DM-scoped typing indicators by name. Item search hits show their key in the result meta and click through to the item's thread view. Scrollback: nearing the top of any chat view loads the previous page (server before-id cursor), keeps the viewport anchored, and marks the beginning of the conversation when history is exhausted. Handler unit-tested; the flows were browser-verified, not automated in CI. Gaps: no reconnect UX polish, no board reordering within a column — this is the dogfood UI, not the real client app |

## Standing rules
1. A subsystem may not be marked done anywhere (README, PRs, timeline) at a
   rating higher than this ledger's.
2. Every PR that touches a subsystem updates its row — reviewers check the row
   against the diff.
3. "DESIGNED" is never evidence. Tests against real infrastructure are.
